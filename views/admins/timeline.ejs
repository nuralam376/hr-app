<% include ../layouts/admin/header %>
<div class="container mt-xs-5 mb-xs-5">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
            <h4>Admin TimeLine</h4>
            <%if (newAdmin.events.length > 0){ %>
                
                <ul class="timeline">
                <% newAdmin.events.forEach(admin => { %>
                    <li>
                        <a target="_blank" href="https://www.totoprayogo.com/#"><%= admin.display_name %></a>
                        <a href="#" class="pull-right"><%= moment(admin.time).format('llll'); %></a>
                        <p><%= admin.description %></p>
                    </li>
                <% }); %>    
                </ul>
            <% } else { %>
                <h1>No Events</h1>
            <% } %>
		</div>
	</div>
</div>

<% include ../layouts/admin/footer %>