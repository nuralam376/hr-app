<% include ../layouts/admin/header %>
<div class="pull-right">
    <% if(admin.isSuperAdmin) { %>
    <a href="/zone/timeline/<%= zone._id %>" class="btn btn-primary">Show Timeline</a>
    <% } %>

    <a href="/zone/edit/<%= zone._id %>" class="btn btn-info">Edit</a>
    <% if(admin.isSuperAdmin) { %>
    <a href="#" class="btn btn-danger delete-zone" data-id="<%= zone._id %>" data-csrf="<%= csrfToken %>">Delete</a>
    <% } %>
</div>
</h1>

<div class="col-md-6">
    <table class="table table-bordered">
        <tr>
            <th></th>
            <td></td>
        </tr>
        <tr>
            <th>Name</th>
            <td><%= zone.name %></td>
        </tr>
        <tr>
            <th>Country</th>
            <td><%= zone.country %></td>
        </tr>
    </table>
</div>

<% include ../layouts/admin/footer %>